services:
  logger.channel.services:
    parent: logger.channel_base
    arguments: ['services']
  services.image_file_custom:
    class: Drupal\services\ImageFileService
    arguments: []
  services.taxonomy_service:
    class: Drupal\services\TaxonomyService
    arguments: []
